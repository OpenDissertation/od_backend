repos:
- repo: local
  hooks:
  - id: ruff-format
    name: ruff format
    entry: uv run ruff format
    language: system
    types: [python]
  - id: ruff-style
    name: ruff style
    entry: uv run ruff check
    language: system
    types: [python]
